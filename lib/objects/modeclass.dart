import 'noteclass.dart';

class Mode {
  final String libelle;
  final Note note;
  final List<double> ecarts;
  final String secondLibelle;
  Mode(this.libelle, this.note, this.ecarts, this.secondLibelle );
}